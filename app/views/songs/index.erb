<div class="light-container clear">
  <h3 class="title-2">The Finer Things Club Playlist, a listing of all the music in the database</h3>

  <p> There are currently <%= @songs.count %> pieces of fine music on the Club's playlist:<p><br>

  <table>
    <thead>
      <th>Name</th>
      <th>Artist</th>
      <th></th>
    </thead>

    <tbody>
      <% @songs.each do |song| %>
        <tr>
          <td><%= song.name %></td>
          <td><%= song.artist %></td>
          <td><button class="button-1"><a href="/songs/<%= song.slug %>"> details</a></button>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
